## Lint / type-check tools (pinned for reproducible CI)
#black==24.4.2
#flake8==7.1.1
#pylint==3.2.7
#mypy==1.10.1
#bandit[toml]==1.7.9
### type stubs for setuptools (type-checking only, not a runtime dep)
#types-setuptools>=68.0.0
#
### Test runner + test-only runtime deps
#pytest==8.2.2
#pytest-asyncio==1.4.0
#openai==1.109.1
#nest_asyncio==1.6.0


## Lint / type-check tools (pinned for reproducible CI)
black
flake8
pylint
mypy
bandit[toml]
## type stubs (type-checking only, not a runtime dep)
types-setuptools
types-tqdm
## Test runner + test-only runtime deps
pytest
pytest-cov
pytest-asyncio
openai
nest_asyncio
## undeclared transitive deps of llm_router_plugins (imported at module load)
numpy
pandas

## fake redis
fakeredis
## lets fakeredis execute the real Lua scripts of the LB strategies
lupa
